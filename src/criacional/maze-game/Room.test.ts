import { describe, it } from "vitest";

describe("Room - classe que representa as salas", () => {
   it.todo("Deve definir o componente Room para se rutilizado no labirinto");

   describe.todo("Deve conhecer os seus possiveis vizinhos.", () => {
      it.todo("Deve conhecer uma outra sala (room)");
      it.todo("Deve conhecer uma parede (wall)");
      it.todo("Deve conhecer uma porta (door) para outra sala (room)");
   });

   it.todo("Cada sala deve ter quatro lados (sides)");
});
