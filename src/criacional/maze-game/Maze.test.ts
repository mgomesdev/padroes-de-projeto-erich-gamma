import { describe, it } from "vitest";

describe("Maze - classe para labirintos (coleção de salas)", () => {
   it.todo("Deve criar a classe Maze (labirinto)");

   it.todo("Deve criar um labirinto como um conjunto de salas (rooms)");

   it.todo("Deve definir os relacionamentos-chave entre os componentes do labirinto.");

   it.todo("Deve armazenar o numero que identifica as sala do labirinto");

   it.todo("Deve achar uma sala especifica, dado um numero de sala, usando sua operação RoomNo.");
});
