import { describe, it } from "vitest";

describe("Classe MazeGame que cria o labirinto (main)", () => {
   it.todo("Deve instanciar as classes necessarias para a criação do labirinto (room, wall, door, maze etc...)");
});
