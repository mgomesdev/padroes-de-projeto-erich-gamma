import { describe, it } from "vitest";

describe("CountingMazeBuilder é um MazeBuilder mais exótico", () => {
   it.todo(
      "Esse construtor nao cria nenhum labirinto, ele apenas conta os diferentes tipos de componentes que teriam sido criados"
   );
   it.todo("O construtor inicia os contadores e as operações de MazeBuilder redefinidas os incrementam adequadamente.");
});
